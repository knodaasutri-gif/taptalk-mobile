コンテキスト設計シート（AIへの指示書） - TapTalk Mobile
1. 今回の依頼内容 (AIに何をしてほしいか)
提供された「TapTalk Mobile 要件定義書」に基づき、発話困難者向けAAC支援Webアプリケーション「TapTalk Mobile」のフロントエンド実装コード（index.html, style.css, script.js）を作成・改修してください。
2. AIに渡す要件 (要件定義書から「今の作業に必要なもの」を抜粋)
【目的】
意思疎通や発話に支援が必要な利用者が、スマートフォンの画面操作（カード選択・定型文作成）および音声入力を用いて、指導員やスタッフへ迅速・正確に連絡（申請・相談・体調・遅刻欠席連絡等）を伝えられるAAC（補助代替医療コミュニケーション）支援ツールを提供する。
【利用者】
* メインユーザー: 障がい者就労支援施設・現場での利用者（発話困難・会話への心理的ハードルがある方）
* サブユーザー: 施設指導員、現場スタッフ、保護者
【必須機能】
* F-01 音声読み上げ（TTS）: window.speechSynthesis を利用した日本語音声読み上げ。
* F-02 拡大ディスプレイ表示: 画面最上部に選択メッセージや音声認識結果を太字ハイコントラスト表示。
* F-03 マルチセンソリーフィードバック: タップ時に視覚（CSS凹み）、聴覚（Web Audio APIチャイム音）、触覚（navigator.vibrate(50)）を同時実行。
* F-04 カテゴリ別カード表示: タブ切替（作業・指示 / 朝礼・挨拶 / 体調・移動 / 雑談・返答 / 📅 お休み・遅刻）による2列レスポンシブグリッド表示。
* F-05 お休み・遅刻の自動文章生成 (核心機能): 日付（今日/明日/明後日/カレンダー）、時間、種別（遅刻/早退/通院/午前休/午後休/終日休み）から「〇月〇日（〇） 〇時〇分頃に到着予定です」等の文章を動的生成し発話＆自動カード化。
* F-06 リアルタイム音声文字起こし: Web Speech API (webkitSpeechRecognition) による音声認識とディスプレイへの即時反映。
* F-07 カード管理機能 (CRUD): カスタムカードの新規作成・編集・個別削除機能。
* F-08 データのローカル保存: LocalStorageへの自動同期・復元。
【データ構造 (LocalStorage model)】
{
 "settings": {
   "soundEnabled": true,
   "vibrateEnabled": true,
   "speechRate": 1.0,
   "speechPitch": 1.0
 },
 "cards": [
   {
     "id": "card_1690000000000",
     "category": "お休み・遅刻",
     "emoji": "⏰",
     "title": "10時30分頃到着",
     "text": "7月30日（木） 10時30分頃に到着予定です。",
     "isCustom": true,
     "createdAt": "2026-07-30T10:30:00Z"
   }
 ]
}

3. 前提・文脈の補足 (デザインの希望・ターゲット層・技術的前提)
【デザイン・UI/UX前提】
* ユーザー特性: ITや細かい操作に不慣れな利用者、または障がい特性を持つ方が使用するため、**ボタンやテキストの視認性を高く（絵文字は32px以上、テキスト対比4.5:1以上）**確保してください。
* 誤操作防止: 画面全体の連続タップによる意図しない拡大防止のため touch-action: manipulation; を適用してください。
【ファイル構造と相互連携前提】
* アプリケーションは index.html（構造）, style.css（デザイン・アニメーション）, script.js（ロジック・ステート管理） の3つのファイルで分離構成されます。
* index.html の <head> 内で <link rel="stylesheet" href="style.css"> によりスタイルを読み込み、</body> 直前に <script src="script.js"></script> を挿入して相互リンクさせる前提で記述してください。
【技術的制約・ブラウザ対策】
1. iOS Safari Autoplay制限対策:
   * 音声読み上げ（TTS）やWeb Audio API（効果音）は、ユーザーのタッチ/クリックイベントの直下で実行または .resume() してください（async/await や setTimeout を挟むとiOSでブロックされます）。
2. Web Speech API 自動停止対策:
   * 無音や省電力による停止（onend）を感知し、UIのトグル状態（マイクON/OFF、点滅演出等）を正確に動期させてください。
3. LocalStorage フォールバック:
   * シークレットモードや容量制限でエラーが起きた場合でもアプリがクラッシュしないよう、try...catch で囲みメモリ上の初期値データで安全に継続動作させてください。
4. 出力形式と制約 (どう出してほしいか、何をしてほしくないか)
【出力形式】
* ファイル分割構成（Multi-File SPA）
   * index.html
   * style.css
   * script.js の3つのファイルにコードを明確に分割して出力・管理すること。
【制約】
* 完全オフライン動作: 外部サーバーへのリクエストや、ネットワーク通信を発生させないこと。
* Pure Vanilla構成:
   * React, Vue, jQuery, Tailwind CSS, Bootstrap などの外部ライブラリ/CSSフレームワークは一切使用禁止。
   * アイコン等も外部フォントに依存せず、標準絵文字（SVG/Emoji）で対応すること。
* モジュール管理・カプセル化:
   * script.js は今後の機能拡張やAI（Codex）によるメンテナンスでコード破壊が起きないよう、状態管理（State）、UI操作（DOM）、音声処理（Audio/TTS）、データ同期（Storage）などを適切な関数・クラス・オブジェクト単位で整理・構築すること。
【やらないこと（スコープ外）】
* バックエンドサーバー構築 / DB（Firebase, RDB等）連携
* ユーザーログイン・会員登録機能
* 複数端末間のリアルタイムクラウド同期
* ネイティブアプリ化（PWAビルドやStore配布設定）
* 画像・動画のストレージ保存機能